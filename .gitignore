# Binaries
*.exe
*.dll
*.so
*.dylib
/bin/
/cmd/tdtpcli/tdtpcli
/cmd/tdtpcli/tdtpcli.exe
/cmd/tdtpserve/tdtpserve
/cmd/tdtpserve/tdtpserve.exe
/tdtpcli
/tdtpserve
/orchestrator

# Test databases
*.db
*.db-journal
test_*.sqlite

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Documentation (keeping in repo but can exclude if needed)
# /docs/
# *.md
# !README.md

# Go
vendor/
# go.sum should be committed for reproducible builds

# Build artifacts
dist/
# Exception: keep tdtp-xray frontend/dist/ for easy building (vanilla JS, no build step)
!cmd/tdtp-xray/frontend/dist/
*.tar.gz
*.zip
# Test data files (generated / large samples)
*.tdtp.xml
# Exception: checked-in test fixtures
!tests/integration/testdata/*.tdtp.xml
!examples/*/output/*.tdtp.xml
*.xlsx
benchmark_*.db
audit.log
test.go
tests.txt

# Local configs (use examples from configs/ instead)
config.yaml
configs/config.yaml
deployments/docker-compose.yml

# funcfinder auto-generated code maps (local only)
docs/analysis/codebase_map.json
docs/analysis/dependencies.json
docs/analysis/.last_update

# Claude Code local files
.claude/

# Access database research artifacts (local only)
*.MDB
*.mdb
*.ldb
*.7z
*.rar
DELO19/
SYSTEM.MDA
SYSTEM_2003.MDA
analyze_relations.vbs
analysis_result.txt
relations_output.csv
relations_utf8.csv
DELO19_schema*
delo19-migration-toolkit/
delo19-migration-toolkit.zip

# SeaweedFS local dev server (local only — never commit production configs)
weed/
filerldb2/
sqlite.test
postgres.test

# Licences and signing keys — never commit.
# tdtpcli looks for ./tdtp.lic in the working directory by default, so the
# natural place to drop a licence is the repo root. The vendor signing key
# (tdtp-license keygen) must stay offline.
tdtp.lic
*.lic
*.priv
vendor.priv

# Python bytecode.
# Пакет bindings/python и наборы tests/cli — обычные Python-модули, поэтому
# __pycache__ появляется в четырёх местах дерева. Правило без слеша в начале
# ловит их все, а не только корневое.
__pycache__/
*.py[cod]
