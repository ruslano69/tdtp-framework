# TDTQL Coverage Analysis — Visual Designer

**Дата:** 2026-02-20
**Статус:** Анализ покрытия спецификации TDTQL в графическом конструкторе

---

## 📊 Общий обзор покрытия

| Категория | Реализовано | Отсутствует | Покрытие |
|-----------|-------------|-------------|----------|
| **Операторы сравнения** | 7/7 | 0 | ✅ 100% |
| **Операторы диапазонов/списков** | 1/3 | 2 | ⚠️ 33% |
| **Операторы паттернов** | 0/2 | 2 | ❌ 0% |
| **Операторы NULL** | 2/2 | 0 | ✅ 100% |
| **Дополнительные проверки** | 2/0 | 0 | ✅ Бонус |
| **Логические операторы** | 2/2 (упрощённо) | 0 | ⚠️ Частично |
| **Сортировка** | 2/2 | 0 | ✅ 100% |
| **Пагинация** | 0/2 | 2 | ❌ 0% |

**Итого:** ~60% базовой функциональности + дополнительные фичи

---

## 1. Операторы сравнения

### ✅ Полностью реализовано

| TDTQL Spec | Visual Designer | Статус | Примечания |
|------------|----------------|--------|-----------|
| `eq` (=) | `=` Equal | ✅ | Идентично |
| `ne` (!=, <>) | `<>` Not Equal | ✅ | Идентично |
| `gt` (>) | `>` Greater Than | ✅ | Идентично |
| `gte` (>=) | `>=` Greater or Equal | ✅ | Идентично |
| `lt` (<) | `<` Less Than | ✅ | Идентично |
| `lte` (<=) | `<=` Less or Equal | ✅ | Идентично |
| `between` | `BW` BETWEEN | ✅ | Есть value2 для диапазона |

**Пример реализации:**
```javascript
<option value="=" selected>= (Equal)</option>
<option value="<>">&lt;&gt; (Not Equal)</option>
<option value=">">&gt; (Greater Than)</option>
// ... остальные операторы
<option value="BW">BETWEEN</option>
```

---

## 2. Операторы диапазонов и списков

### ✅ Реализовано

| Оператор | TDTQL | Visual Designer | Статус |
|----------|-------|----------------|--------|
| **BETWEEN** | `<Filter operator="between" value="18" value2="65"/>` | `BW` с полями value1 и value2 | ✅ |

**Пример:**
```javascript
currentFilter = { operator: 'BW', value: '18', value2: '65', logic: 'AND' }
// → age BETWEEN 18 AND 65
```

### ❌ Отсутствуют

| Оператор | TDTQL Spec | Что нужно добавить |
|----------|------------|-------------------|
| **IN** | `<Filter operator="in" value="Moscow,SPb,Kazan"/>` | Dropdown с multiple select или textarea |
| **NOT IN** | `<Filter operator="not_in" value="deleted,archived"/>` | Аналогично IN |

**Пример спецификации:**
```xml
<Filter field="city" operator="in" value="Moscow,SPb,Kazan"/>
<!-- SQL: WHERE city IN ('Moscow', 'SPb', 'Kazan') -->
```

**Предлагаемая реализация:**
```javascript
<optgroup label="Lists">
  <option value="IN">IN (comma-separated)</option>
  <option value="NOT_IN">NOT IN (comma-separated)</option>
</optgroup>

// UI для IN:
<input type="text" placeholder="Moscow,SPb,Kazan" />
<small>Comma-separated values</small>
```

---

## 3. Операторы паттернов

### ❌ Полностью отсутствуют

| Оператор | TDTQL Spec | Применение |
|----------|------------|-----------|
| **LIKE** | `<Filter operator="like" value="%@example.com"/>` | Поиск по email, имени |
| **NOT LIKE** | `<Filter operator="not_like" value="test%"/>` | Исключение паттернов |

**Wildcards:**
- `%` — любое количество символов
- `_` — один символ

**Пример спецификации:**
```xml
<Filter field="email" operator="like" value="%@example.com"/>
<!-- SQL: WHERE email LIKE '%@example.com' -->
```

**Предлагаемая реализация:**
```javascript
<optgroup label="Pattern Matching">
  <option value="LIKE">LIKE (%, _)</option>
  <option value="NOT_LIKE">NOT LIKE</option>
</optgroup>

// UI hint:
<input type="text" placeholder="%@example.com" />
<small>Use % (any chars) and _ (one char)</small>
```

**Примеры use cases:**
- Поиск email'ов домена: `email LIKE '%@company.com'`
- Поиск телефонов: `phone LIKE '+7%'`
- Исключение тестовых записей: `username NOT LIKE 'test%'`

---

## 4. Операторы NULL

### ✅ Полностью реализовано

| TDTQL Spec | Visual Designer | Статус |
|------------|----------------|--------|
| `is_null` | `IS_NULL` | ✅ |
| `is_not_null` | `IS_NOT_NULL` | ✅ |

**Пример реализации:**
```javascript
<optgroup label="NULL checks">
  <option value="IS_NULL">IS NULL</option>
  <option value="IS_NOT_NULL">IS NOT NULL</option>
</optgroup>
```

---

## 5. Дополнительные проверки (Бонус)

### ✅ Реализовано сверх спецификации

| Оператор | Visual Designer | Применение |
|----------|----------------|-----------|
| **IS_EMPTY** | `= ''` (Empty string) | Проверка пустой строки |
| **IS_NOT_EMPTY** | `<> ''` (Not empty) | Проверка непустой строки |

```javascript
<optgroup label="Empty string checks">
  <option value="IS_EMPTY"> = '' (Empty string)</option>
  <option value="IS_NOT_EMPTY">&lt;&gt; '' (Not empty string)</option>
</optgroup>
```

**Отличие от NULL:**
- `NULL` ≠ пустая строка `''`
- `IS_EMPTY` полезен для TEXT полей без NOT NULL constraint

---

## 6. Логические операторы

### ⚠️ Упрощённая реализация

| Фича | TDTQL Spec | Visual Designer | Статус |
|------|------------|----------------|--------|
| **AND** | `<And>` группа фильтров | Каждый filter имеет `logic: 'AND'` | ⚠️ Упрощено |
| **OR** | `<Or>` группа фильтров | Каждый filter имеет `logic: 'OR'` | ⚠️ Упрощено |
| **Вложенные группы** | `<And><Or>...</Or></And>` | ❌ Нет | ❌ Отсутствует |

### Текущая реализация (упрощённая)

**Visual Designer:**
```javascript
{
  operator: '>=',
  value: '1000',
  logic: 'AND'  // Как этот фильтр комбинируется с другими
}
```

**Генерируемый SQL (упрощённый):**
```sql
WHERE balance >= 1000 AND age >= 18 OR city = 'Moscow'
-- Линейная цепочка без группировки скобками
```

### TDTQL Spec (полная)

**Вложенные группы:**
```xml
<Filters>
  <And>
    <Filter field="is_active" operator="eq" value="1"/>
    <Or>
      <Filter field="city" operator="eq" value="Moscow"/>
      <Filter field="city" operator="eq" value="SPb"/>
    </Or>
  </And>
</Filters>
```

**SQL с группировкой:**
```sql
WHERE is_active = 1 AND (city = 'Moscow' OR city = 'SPb')
```

### Что отсутствует

❌ **Группировка фильтров в скобки**
❌ **Вложенные AND/OR блоки**
❌ **Визуальный редактор для сложных условий**

### Предлагаемое решение

**Вариант 1: Простая группировка (Quick Win)**
```
Добавить checkbox "Group with next filter":
┌─────────────────────────────────┐
│ balance >= 1000       [AND]     │
│ ☑ Group with next               │
├─────────────────────────────────┤
│ age >= 18            [OR]       │
│ ☐ Group with next               │
└─────────────────────────────────┘

→ WHERE (balance >= 1000 AND age >= 18) OR ...
```

**Вариант 2: Advanced Query Builder (Full TDTQL)**
```
Visual tree builder для вложенных групп:
AND
├─ balance >= 1000
└─ OR
   ├─ city = Moscow
   └─ city = SPb
```

---

## 7. Сортировка (OrderBy)

### ✅ Полностью реализовано

| TDTQL Spec | Visual Designer | Статус |
|------------|----------------|--------|
| `ASC` | ↑ Ascending (ASC) | ✅ |
| `DESC` | ↓ Descending (DESC) | ✅ |
| Множественная сортировка | Priority = field order in table | ✅ |

**Пример реализации:**
```javascript
<select id="filterSort">
  <option value="">— No sort</option>
  <option value="ASC">↑ Ascending (ASC)</option>
  <option value="DESC">↓ Descending (DESC)</option>
</select>
```

**Множественная сортировка:**
```sql
-- Если несколько полей имеют sort:
ORDER BY balance DESC, created_at ASC
-- Приоритет = порядок полей в таблице
```

**TDTQL эквивалент:**
```xml
<OrderBy>
  <Fields>
    <OrderField name="balance" direction="DESC"/>
    <OrderField name="created_at" direction="ASC"/>
  </Fields>
</OrderBy>
```

---

## 8. Пагинация

### ❌ Полностью отсутствует

| TDTQL Spec | Visual Designer | Статус |
|------------|----------------|--------|
| `<Limit>` | ❌ Нет | ❌ |
| `<Offset>` | ❌ Нет | ❌ |

**TDTQL спецификация:**
```xml
<Limit>100</Limit>
<Offset>200</Offset>
<!-- SQL: LIMIT 100 OFFSET 200 -->
```

**Где это нужно:**
- Preview данных (LIMIT 10 для быстрого просмотра)
- Пагинация больших таблиц
- Batch processing

**Предлагаемая реализация:**
```
В Step 6 (Performance & Settings) или Step 7 (Review):
┌──────────────────────────────┐
│ Preview Limit:  [100    ] ▼ │
│ ☑ Enable pagination         │
│   Page size: [1000     ]    │
│   Offset:    [0        ]    │
└──────────────────────────────┘
```

---

## 9. Структура запроса (Query)

### Текущая архитектура

**Visual Designer хранит фильтры по-полям:**
```javascript
canvasDesign = {
  tables: [{
    sourceName: "users",
    fields: [{
      name: "balance",
      filter: { operator: '>=', value: '1000', logic: 'AND' },
      sort: 'DESC'
    }]
  }]
}
```

**TDTQL ожидает централизованную структуру:**
```xml
<Query language="TDTQL" version="1.0">
  <Filters>
    <And>
      <Filter field="balance" operator="gte" value="1000"/>
      <Filter field="is_active" operator="eq" value="1"/>
    </And>
  </Filters>
  <OrderBy field="balance" direction="DESC"/>
  <Limit>100</Limit>
</Query>
```

### ⚠️ Проблема трансформации

**Что нужно:**
1. Собрать все поля с фильтрами из всех таблиц
2. Сгруппировать по logic (AND/OR)
3. Сгенерировать корректную TDTQL структуру

**Пример:**
```javascript
// Input (Visual Designer):
table1.fields[0]: { name: 'balance', filter: { operator: '>=', value: '1000', logic: 'AND' }}
table1.fields[1]: { name: 'age', filter: { operator: '>=', value: '18', logic: 'OR' }}

// Output (TDTQL):
<Filters>
  <And>
    <Filter field="balance" operator="gte" value="1000"/>
  </And>
  <Or>
    <Filter field="age" operator="gte" value="18"/>
  </Or>
</Filters>
// ⚠️ Но это некорректно! Должно быть:
// WHERE balance >= 1000 AND age >= 18  (если оба AND)
// Или с группировкой для OR
```

---

## 10. Маппинг операторов

### Visual Designer → TDTQL

| UI Operator | TDTQL Operator | SQL |
|-------------|----------------|-----|
| `=` | `eq` | `=` |
| `<>` | `ne` | `<>` или `!=` |
| `>` | `gt` | `>` |
| `>=` | `gte` | `>=` |
| `<` | `lt` | `<` |
| `<=` | `lte` | `<=` |
| `BW` | `between` | `BETWEEN ... AND ...` |
| `IS_NULL` | `is_null` | `IS NULL` |
| `IS_NOT_NULL` | `is_not_null` | `IS NOT NULL` |
| ❌ `IN` | ❌ `in` | `IN (...)` |
| ❌ `NOT_IN` | ❌ `not_in` | `NOT IN (...)` |
| ❌ `LIKE` | ❌ `like` | `LIKE '...'` |
| ❌ `NOT_LIKE` | ❌ `not_like` | `NOT LIKE '...'` |
| `IS_EMPTY` (бонус) | — | `= ''` |
| `IS_NOT_EMPTY` (бонус) | — | `<> ''` |

---

## 11. Приоритетный список доработок

### 🔴 Критично (без этого TDTQL неполный)

1. **Операторы LIKE / NOT LIKE**
   - Частота использования: очень высокая
   - Use cases: поиск по email, телефонам, именам
   - Сложность: низкая (1 день)
   - Impact: высокий

2. **Операторы IN / NOT IN**
   - Частота использования: высокая
   - Use cases: фильтрация по спискам городов, статусов
   - Сложность: средняя (2 дня)
   - Impact: высокий

3. **Пагинация (LIMIT/OFFSET)**
   - Частота использования: критическая для preview
   - Use cases: быстрая проверка данных
   - Сложность: низкая (1 день)
   - Impact: средний

### 🟡 Важно (улучшает юзабилити)

4. **Группировка фильтров (скобки)**
   - Частота использования: средняя
   - Use cases: сложные условия с AND/OR
   - Сложность: высокая (5 дней)
   - Impact: высокий

5. **Visual Query Builder**
   - Частота использования: низкая (для advanced users)
   - Use cases: очень сложные запросы
   - Сложность: очень высокая (10+ дней)
   - Impact: средний

### 🟢 Улучшения (nice to have)

6. **Автокомплит для значений**
   - Пример: при фильтрации по city показать [Moscow, SPb, Kazan]
   - Сложность: средняя (3 дня)

7. **Валидация типов**
   - Пример: для INTEGER не разрешать вводить текст
   - Сложность: низкая (1 день)

8. **Saved filters templates**
   - Сохранение частых фильтров
   - Сложность: средняя (2 дня)

---

## 12. Roadmap реализации

### Phase 1: Critical Operators (1 неделя)
- [ ] LIKE / NOT LIKE операторы
- [ ] IN / NOT IN операторы
- [ ] LIMIT/OFFSET для preview
- [ ] Тесты для новых операторов

### Phase 2: Query Builder (2 недели)
- [ ] Рефакторинг хранения фильтров (централизация)
- [ ] Группировка фильтров (скобки)
- [ ] Корректная генерация TDTQL из UI
- [ ] Preview сгенерированного SQL

### Phase 3: UX Improvements (1 неделя)
- [ ] Автокомплит значений
- [ ] Валидация по типам полей
- [ ] Подсказки по wildcards (%, _)
- [ ] Saved filter templates

---

## 13. Примеры недостающих use cases

### ❌ Сейчас невозможно:

**1. Поиск email'ов домена**
```sql
WHERE email LIKE '%@company.com'
```

**2. Фильтрация по списку городов**
```sql
WHERE city IN ('Moscow', 'SPb', 'Kazan', 'Novosibirsk')
```

**3. Сложное условие с группировкой**
```sql
WHERE is_active = 1 AND (city = 'Moscow' OR city = 'SPb')
```

**4. Preview первых 10 записей**
```sql
SELECT * FROM users LIMIT 10
```

### ✅ После доработок:

Всё вышеперечисленное станет доступно через визуальный интерфейс.

---

## 14. Заключение

### Итоговое покрытие TDTQL: ~60%

✅ **Сильные стороны:**
- Все операторы сравнения
- NULL проверки
- BETWEEN
- Сортировка (в т.ч. множественная)
- Бонусные IS_EMPTY/IS_NOT_EMPTY

❌ **Критичные пробелы:**
- LIKE / NOT LIKE (текстовый поиск)
- IN / NOT IN (списки)
- Группировка фильтров (скобки)
- Пагинация (LIMIT/OFFSET)

⚠️ **Архитектурные ограничения:**
- Упрощённая модель AND/OR (без вложенности)
- Фильтры привязаны к полям, а не централизованы
- Нет визуализации итогового TDTQL

### Рекомендации:

1. **Quick Wins (1-2 недели):**
   - Добавить LIKE/NOT LIKE
   - Добавить IN/NOT IN
   - Добавить LIMIT для preview

2. **Strategic (1 месяц):**
   - Рефакторинг архитектуры фильтров
   - Query Builder с группировкой
   - TDTQL preview в Step 7

3. **Future:**
   - Advanced Query Builder (tree view)
   - Saved filter templates
   - Auto-suggestions
